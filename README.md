# ikemura-farm
- いけむら農園のネットショップ

# ディレクトリ構成
- index.html
- style.css
- img/

# git エイリアス
main に merge 設定
```git config --global alias.deploy "!git switch main && git pull && git merge --no-ff test && git push"```

実行のコマンド
```git deploy```
